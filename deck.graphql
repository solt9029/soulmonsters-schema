type Deck implements Node {
  id: ID!
  userId: String!
  name: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

input DeckCreateInput {
  name: String!
}

type Mutation {
  createDeck(data: DeckCreateInput!): Deck!
}

type Query {
  decks: [Deck!]!
}
